name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: vijayendiranakash
          password: dckr_pat_ronAh9EL_WR1bqTpFyx95NFh5Ng@123

      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/saas-backend:latest .

      - name: Push Docker Image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/saas-backend:latest
